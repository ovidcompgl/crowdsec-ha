---
- name: Configurare patroni.yml
  template:
    src: patroni.yml.j2
    dest: /etc/patroni/patroni.yml
    owner: postgres
    group: postgres
    mode: '0640'
  notify: restart patroni

- name: Creare unit systemd pentru Patroni
  template:
    src: patroni.service.j2
    dest: /etc/systemd/system/patroni.service
    owner: root
    group: root
    mode: '0644'
  notify:
    - reload systemd
    - restart patroni

- name: Reload systemd daemon
  systemd:
    daemon_reload: yes

- name: Pornire si activare Patroni
  systemd:
    name: patroni
    state: started
    enabled: yes

# Asteapta pana PostgreSQL local porneste pe nodul bootstrap
- name: Asteptare PostgreSQL local disponibil
  wait_for:
    host: 127.0.0.1
    port: "{{ postgresql_port }}"
    delay: 5
    timeout: 180

# Creare utilizatori si baza de date o singura data, pe primul nod
- name: Creare utilizator de replicare
  become: yes
  become_user: postgres
  postgresql_user:
    name: "{{ postgresql_replication_user }}"
    password: "{{ postgresql_replication_password }}"
    role_attr_flags: "LOGIN,REPLICATION"
    state: present
  run_once: true
  delegate_to: "{{ groups['crowdsec_containers'] | first }}"

- name: Creare baza de date CrowdSec
  become: yes
  become_user: postgres
  postgresql_db:
    name: "{{ postgresql_crowdsec_db }}"
    state: present
  run_once: true
  delegate_to: "{{ groups['crowdsec_containers'] | first }}"

- name: Creare utilizator CrowdSec
  become: yes
  become_user: postgres
  postgresql_user:
    name: "{{ postgresql_crowdsec_user }}"
    password: "{{ postgresql_crowdsec_password }}"
    db: "{{ postgresql_crowdsec_db }}"
    priv: "ALL"
    state: present
  run_once: true
  delegate_to: "{{ groups['crowdsec_containers'] | first }}"
